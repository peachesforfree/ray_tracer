#include "../includes/ray_tracer.h"


int     rectangle_hit(const t_ray *r, float t_min, float t_max, t_hit_record *rec, void *ptr)
{
    t_rectangle     *object;

    object = ptr;
    float   denom;

    denom = dot();
    if ()
    return (1);
}

t_vec3  translation(t_vec3 origin, t_vec3 translate)
{
    t_vec3  result;
}

int     alloc_rectangle(t_vec3 loc, t_vec3 rot, t_vec3 size, t_vec3 color)
{

}